NODE_ENV=production
PORT=3000

# Security / auth
SMT_BACKEND_API_KEY=productionsmt-api-key
JWT_SECRET=random_jwt_secret_4321
SMT_ENCRYPTION_KEY=productionsmt-dev-encryption-secret
JWT_EXPIRES_IN=30d

# CORS – allow Flutter app requests from this IP (add domain later)
CORS_ALLOWED_ORIGINS=http://213.199.35.225

# Storage
SMT_DB_PATH=./data/smt.db
SMT_SESSION_STORE=memory
REDIS_URL=redis://127.0.0.1:6379
SMT_REDIS_KEY_PREFIX=smt:session:

# Provider
SMT_PROVIDER=unofficial
SMT_UNOFFICIAL_BASE_URL=https://www.smartmetertexas.com
SMT_UNOFFICIAL_ALLOW_ENV_FALLBACK=false

# Background sync
SMT_SYNC_ENABLED=true
SMT_SYNC_INTERVAL_MS=1800000

# Rate limiting
API_RATE_LIMIT_MAX=300
API_RATE_LIMIT_WINDOW_MS=60000
API_RATE_LIMIT_USAGE_HISTORY_MAX=600
SMT_ODR_MAX_PER_HOUR=2
SMT_ODR_MAX_PER_DAY=24

# Role-based admin access (comma-separated)
ADMIN_SMT_USERNAMES=admin
# ADMIN_USER_IDS=1

# Demo admin login (leave empty in production)
DEMO_ADMIN_USERNAME=
DEMO_ADMIN_PASSWORD=
